import "server-only"

/**
 * creates and inserts a password token into the database
 */
export const insertForgotPasswordToken = async (email: string) => {
  throw new Error("Method not implemented." + email + "could not be reset")
}
