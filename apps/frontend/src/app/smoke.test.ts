import { describe, expect, it } from "vitest"

// TODO: Remove when we have a proper test, this is just a placeholder otherwise the test runner will fail
describe("smoke test", () => {
  it("should pass", () => {
    expect(true).toBe(true)
  })
})
