{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["@repo/test-config#build", "^build"]
    },
    "build:workers": {
      "dependsOn": ["@repo/test-config#build"],
      "outputs": [".open-next/**"]
    }
  }
}
