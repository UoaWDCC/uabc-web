{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "@repo/test-config#build", "@repo/shared#build"],
      "outputs": ["out/**"]
    }
  }
}
