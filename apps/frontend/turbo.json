{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["@repo/test-config#build", "^build"]
    },
    "worker:build": {
      "dependsOn": ["@repo/test-config#build", "^worker:build"]
    }
  }
}
