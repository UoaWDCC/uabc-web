{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["@repo/test-config#build", "^build"]
    },
    "cf-typegen": {
      "dependsOn": ["^cf-typegen", "@repo/test-config#build"]
    },
    "deploy": {
      "dependsOn": ["^deploy", "frontend#build"]
    }
  }
}
