{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "@repo/test-config#build"],
      "env": ["NODE_ENV", "NEXT_PUBLIC_URL", "NEXT_PUBLIC_API_URL"],
      "outputs": ["out/**"]
    }
  }
}
