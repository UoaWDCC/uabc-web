{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["@repo/test-config#build", "frontend#cf-typegen", "^build"]
    },
    "cf-typegen": {
      "dependsOn": ["@repo/test-config#build", "^cf-typegen"]
    },
    "deploy": {
      "dependsOn": ["frontend#build", "^deploy"]
    }
  }
}
