{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["storybook-static/**"]
    },
    "start": {
      "dependsOn": ["^start"],
      "inputs": ["storybook-static/**"]
    }
  }
}
